{
  "name": "micdrop",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "prettier": "3.8.1",
    "turbo": "^2.8.20"
  },
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  }
}
